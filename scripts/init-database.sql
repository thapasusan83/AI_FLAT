-- Create database if it doesn't exist
CREATE DATABASE IF NOT EXISTS flat_rental_db;
USE flat_rental_db;

-- Note: Prisma will handle table creation via migrations
-- This script is just for initial database setup

-- You can run this script in your MySQL client or via command line:
-- mysql -u root -p < scripts/init-database.sql
